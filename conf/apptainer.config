apptainer {
    enabled = true
    cacheDir = "apptainer-images"
    autoMounts = true
    containerOptions = { workflow.containerEngine == "apptainer" ? '--bind /tmp' : '' }
    envWhitelist = []
    pullTimeout = 2700
    apptainer_pull_docker_container = false
} 